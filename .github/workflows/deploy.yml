name: ONZA Bot CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run tests
      run: |
        python -m py_compile main.py
        python -m py_compile config.py
        python -m py_compile utils.py
        python -m py_compile data_manager.py
        python -m py_compile events/moderation_events.py
        python -m py_compile events/bot_events.py
        python -m py_compile events/channels.py
        python -m py_compile events/interactive_messages.py
        python -m py_compile commands/admin.py
        python -m py_compile commands/user.py
        python -m py_compile commands/tickets.py
        python -m py_compile commands/moderation.py
        python -m py_compile commands/publication.py
        python -m py_compile commands/reviews.py
        python -m py_compile fortnite/__init__.py
        python -m py_compile fortnite/fortnite_cog.py
        python -m py_compile fortnite/auth.py
        python -m py_compile fortnite/accounts.py
        python -m py_compile fortnite/friends.py
        python -m py_compile fortnite/gifting.py
        python -m py_compile fortnite/store.py
        python -m py_compile fortnite/security.py
        python -m py_compile fortnite/rate_limiter.py
        python -m py_compile fortnite/tos_validator.py
    
    - name: Check code quality
      run: |
        pip install flake8
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deploy to Render
      run: |
        echo "🚀 ONZA Bot CI/CD completed successfully!"
        echo "✅ Code quality checks passed"
        echo "📋 Render will automatically deploy from GitHub"
        echo "🌐 Bot will be available at: https://onza-bot.onrender.com/"
